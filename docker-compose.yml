version: '3'

services:
  postgresql:
    image: 'postgres'
    ports:
      - '5432:5432'
    environment:
      POSTGRES_USER: "${DATABASE_ID}"
      POSTGRES_PASSWORD: "${DATABASE_PASS}"
      POSTGRES_DB: "${DATABASE_NAME}"


